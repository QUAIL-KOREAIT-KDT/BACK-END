# .gitignore
# gitginore로 최대한 업로드하는 불필요한 파일을 제외합시다!
# 1. 파이썬 필수 무시 항목 (모든 폴더 적용)
__pycache__/
*.py[cod]
*$py.class
# *.txt

# 2. 가상환경 및 환경변수 (보안 필수)
venv/
.env
.env.local

# 3. 프로젝트 데이터 및 임시 파일
db_data/
.DS_Store

# 4. IDE 설정 파일 (VSCode, Pycharm 등)
.vscode/
.idea/