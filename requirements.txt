# ==========================================
# QUAIL Project Dependency List
# Environment: Local & EC2 t3a.medium (Ubuntu)
# Python: 3.10+ (Recommended)
# ==========================================

# [1] Backend Core & API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6
requests>=2.31.0

# [2] Database (Async MySQL)
sqlalchemy>=2.0.0
aiomysql>=0.2.0
cryptography

# [3] Auth & Security (JWT)
python-jose[cryptography]
passlib[bcrypt]

# [4] AWS Cloud (S3 Image Upload)
boto3>=1.28.0

# [5] AI - Computer Vision (YOLO)
ultralytics>=8.0.0
opencv-python-headless
pillow>=10.0.0

# [6] AI - RAG & Search (OpenAI, Vector DB)
openai>=1.0.0
chromadb>=0.4.0
tiktoken
sentence-transformers

# ==========================================
# [설치 가이드]
# 0. 파이썬 버전: 3.11.9 (3.14버전은 설치 안됨)
# 1. 가상환경 생성: python -m venv venv
# 2. 가상환경 활성: source venv/bin/activate (Mac/Linux) or venv\Scripts\activate (Win)
# 3. 전체 설치: pip install -r requirements.txt
# 4. 서버 실행: uvicorn app.main:app --reload
# 5. API 실행(swagger): http://127.0.0.1:8000/docs
# ==========================================